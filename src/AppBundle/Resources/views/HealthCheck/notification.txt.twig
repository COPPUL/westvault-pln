This is an automated notification. 

The following OJS Institutions have not contacted the PKP PLN Staging Server in more
than {{ days }} days. 

{% for institution in institutions %}
    {{ institution.title }} - {{ institution.url }}
    Most recent contact: {{ institution.contacted|date }}
    {% if institution.publisherName %}Published by {{ institution.publisherName }}{% endif %}
    
    {% if institution.publisherUrl %}{{ institution.publisherUrl }}{% endif %}
    
    This institution has made {{ institution.deposits|length }} deposits.
{% endfor %}

Please check with the institution and/or the publishers to see if the institution
should be triggered.

Institution status can be updated at {{ url('institution') }}.